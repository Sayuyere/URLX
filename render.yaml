services:
  - type: web
    name: urlx
    env: go
    buildCommand: go build -o urlx main.go
    startCommand: ./urlx
    plan: free
    envVars:
      - key: DATABASE_UR
        fromDatabase:
          name: urlx-db-env
          property: connectionString
databases:
  - name: urlx-db
    plan: free
    databaseName: urlx
    user: urlx_user
